const { Router } = require('express');

const router = Router();

// findCategory

// findCategories

// createCategory

// updateCategory

// deleteCategory

module.exports = {
  categoriesRouter: router,
};
